[package]
name = "mempool"
version = "0.1.0"
edition = "2021"

[dependencies]
ethers = { version = "2.0.11", features = ["abigen", "ws", "ethers-solc"] }
tokio = { version = "1.31", features = ["full"] }
tokio-stream= {version = "0.1", features = ["sync"]}
dotenv = "0.15"
